---
title: /validate-team-dashboard
description: Aggregate team validation posture into a shared dashboard showing coverage, scores, regressions, and ownership across all registered projects
---

import { Aside } from '@astrojs/starlight/components';

Aggregate validation posture across all team projects into a single shared dashboard. Shows coverage metrics, posture scores, regression trends, and ownership assignments so engineering leads can identify under-validated projects at a glance.

## Full Reference

The full command definition — including flags, pipeline stages, and examples —
lives in the repo:

- [`commands/validate-team-dashboard.md`](https://github.com/krzemienski/validationforge/blob/main/commands/validate-team-dashboard.md)

<Aside type="note" title="Thin reference page">
  This page is a thin index entry. The canonical source for `/validate-team-dashboard` is the
  `commands/validate-team-dashboard.md` file in the ValidationForge repo. Slash commands are
  executed by Claude Code — the `.md` file is the executable spec.
</Aside>
